# ── 웹 프레임워크 ──────────────────────────────────────────
fastapi==0.115.5          # 파이썬 웹 프레임워크 (Flask보다 빠르고 자동 문서화 지원)
uvicorn[standard]==0.32.0 # FastAPI를 실제로 실행시켜주는 서버 (식당의 홀 직원 역할)

# ── 데이터베이스 ───────────────────────────────────────────
sqlalchemy==2.0.36        # 파이썬 코드로 SQL을 다루게 해주는 ORM 라이브러리
asyncpg==0.30.0           # PostgreSQL과 비동기로 통신하는 드라이버 (Python 3.13 지원)
alembic==1.14.0           # DB 스키마 변경 이력을 관리하는 마이그레이션 도구 (DB용 git)

# ── 설정 / 환경변수 ────────────────────────────────────────
pydantic==2.10.3          # 데이터 유효성 검사 라이브러리 (입력값 틀리면 자동으로 에러)
pydantic-settings==2.7.0  # .env 파일을 자동으로 읽어서 설정값으로 변환해주는 도구

# ── 인증 ──────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT 토큰 생성·검증 라이브러리 (회원 신분증 발급)
passlib[bcrypt]==1.7.4            # 비밀번호 암호화 라이브러리 (절대 평문 저장 금지)
bcrypt==4.0.1                     # passlib과 호환되는 버전 (5.x는 passlib과 충돌)

# ── AI ────────────────────────────────────────────────────
groq==0.9.0               # Groq API 공식 SDK (Llama 등 오픈소스 모델을 초고속으로 실행)

# ── 유틸리티 ──────────────────────────────────────────────
python-multipart==0.0.12  # Form 데이터(파일 업로드 등) 파싱에 필요
httpx==0.27.2             # 비동기 HTTP 클라이언트 (외부 API 호출용)
email-validator==2.2.0    # EmailStr 타입 검증에 필요 (pydantic이 내부적으로 사용)
greenlet==3.2.2           # SQLAlchemy 비동기 실행에 필요한 코루틴 라이브러리
